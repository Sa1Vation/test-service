spring:
  cloud:
    consul:
      host: 192.168.0.111
      port: 8500
      discovery:
        instance-id: ${spring.application.name}:${server.port}
        prefer-ip-address: true
  application:
    name: serviceB